version: '1.2'
project_name: cc-spec
tech_requirements_sources:
- .claude/CLAUDE.md
- AGENTS.md
- pyproject.toml
- package.json
subagent:
  max_concurrent: 10
  common:
    model: sonnet
    timeout: 300000
    permissionMode: default
agents:
  enabled:
  - codex
  default: codex
checklist:
  pass_threshold: 80
  auto_retry: false
