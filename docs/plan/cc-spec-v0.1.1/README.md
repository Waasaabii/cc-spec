# cc-spec v1.1 设计文档

## 版本状态

🟩 **已完成** - 完成日期: 2025-12

**后续版本**: [cc-spec v1.2](../cc-spec-v1.2/README.md)

## 概述

**cc-spec v1.1** 是 cc-spec 的功能增强版本，新增统一的列表/回溯能力、配置热更新机制，以及 Subagent 公共配置支持。

## 版本定位

| 版本 | 定位 |
|------|------|
| **v1.0** | 基础工作流：init → specify → clarify → plan → apply → checklist → archive |
| **v1.1** | 增强功能：list + goto + update + ID 回溯 + Subagent 公共配置 |

## 新增功能

### 命令新增

```
cc-spec list [type]     → 列出变更/任务/规格/归档
cc-spec goto {ID}       → 智能跳转到指定变更或任务
cc-spec update [opts]   → 更新斜杠命令、Subagent 配置、AI 工具
```

### 现有命令增强

```
cc-spec specify [ID]    → 支持 ID 回溯编辑
cc-spec clarify [ID]    → 支持 ID 指定变更
cc-spec plan [ID]       → 支持 ID 重新规划
cc-spec apply [ID]      → 支持 ID 指定执行
cc-spec checklist [ID]  → 支持 ID 指定验收
cc-spec archive [ID]    → 支持 ID 指定归档
```

### 斜杠命令（17+ AI 工具）

```
/speckit.list           → 列出变更
/speckit.goto {ID}      → 跳转到变更
/speckit.update         → 更新配置
```

## 文档索引

| 文档 | 内容 | 状态 |
|------|------|------|
| [01-背景与目标](./01-背景与目标.md) | v1.1 业务目的、上线标准、成功指标 | ✅ |
| [02-现状分析](./02-现状分析.md) | v1.0 现状、缺口分析、用户反馈 | ✅ |
| [03-设计方案](./03-设计方案.md) | list/goto/update 命令设计、ID 机制、Subagent 配置 | ✅ |
| [04-实施步骤](./04-实施步骤.md) | Gate 分阶段、验收标准 | ✅ |
| [05-任务拆分](./05-任务拆分.md) | Wave 任务规划、Task 详情 | ✅ |

## 关键指标

| 指标 | v1.0 | v1.1 目标 |
|------|------|-----------|
| 命令数量 | 8 个 | 11 个 (+3) |
| ID 回溯 | ❌ | ✅ 全命令支持 |
| 配置热更新 | ❌ | ✅ `cc-spec update` |
| Subagent 公共配置 | ❌ | ✅ common/profiles |
| 斜杠命令 | 基础 | 增强 (+list/goto/update) |

## 技术栈

```yaml
# 继承 v1.0 技术栈
语言: Python 3.11+
包管理: uv
CLI 框架: typer
终端美化: rich

# v1.1 新增模块
ID 管理: core/id_manager.py
命令生成: core/command_generator.py
Subagent 配置: subagent/config.py
```

## 依赖关系

```
cc-spec v1.0 (基础功能)
    ↓
cc-spec v1.1 (本次增强)
    ├── list 命令
    ├── goto 命令
    ├── update 命令
    ├── ID 管理机制
    ├── Subagent 公共配置
    └── 斜杠命令模板更新
```

## 参考资源

- `docs/plan/cc-spec/` - v1.0 设计文档
- `src/cc_spec/` - 现有实现代码
- `reference/spec-kit/` - Spec-Kit 命令模板参考

---

*文档创建时间: 2025-12*
