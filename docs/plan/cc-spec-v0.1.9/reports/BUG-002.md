# BUG-002 Report

修复 Tauri Viewer 目录选择与 stream.json 读取失败的问题，并放行 SSE 连接。

## 变更摘要
- 新增 Rust `load_manifest` command 读取并解析 `.cc-spec/runtime/codex/stream.json`。
- 前端改用 `invoke` 调用 Rust command，避免 fs plugin 失败。
- tauri.conf.json 增加 capabilities 与 CSP（允许 SSE/开发连接）。

## 影响文件
- apps/cc-spec-tool/package.json
- apps/cc-spec-tool/src/App.tsx
- apps/cc-spec-tool/src-tauri/tauri.conf.json
- apps/cc-spec-tool/src-tauri/src/main.rs

## 测试
- 未运行（建议 tauri dev 验证）。