# 05 - 任务拆分

## 概览

| Wave | Task-ID | Profile | 预估 | 状态 | 依赖 |
|------|---------|---------|------|------|------|
| 0 | 01-PREP | quick | 20k | 🟦 空闲 | - |
| 1 | 02-CONFIG-SCORING | common | 30k | 🟦 空闲 | 01-PREP |
| 1 | 03-CONFIG-LOCK | common | 25k | 🟦 空闲 | 01-PREP |
| 2 | 04-SCORING-REFACTOR | heavy | 60k | 🟦 空闲 | 02-CONFIG-SCORING |
| 2 | 05-LOCK-MODULE | common | 50k | 🟦 空闲 | 03-CONFIG-LOCK |
| 2 | 06-STATE-ENHANCE | common | 30k | 🟦 空闲 | 01-PREP |
| 3 | 07-EXECUTOR-LOCK | common | 40k | 🟦 空闲 | 05-LOCK-MODULE, 06-STATE-ENHANCE |
| 3 | 08-EXECUTOR-RESULT | quick | 25k | 🟦 空闲 | 06-STATE-ENHANCE |
| 4 | 09-CMD-CHECKLIST | common | 45k | 🟦 空闲 | 04-SCORING-REFACTOR |
| 4 | 10-CMD-APPLY | common | 40k | 🟦 空闲 | 07-EXECUTOR-LOCK |
| 5 | 11-QUICKDELTA-ENHANCE | common | 35k | 🟦 空闲 | 01-PREP |
| 6 | 12-TEST-SCORING | quick | 30k | 🟦 空闲 | 09-CMD-CHECKLIST |
| 6 | 13-TEST-LOCK | quick | 30k | 🟦 空闲 | 10-CMD-APPLY |
| 6 | 14-TEST-QUICKDELTA | quick | 25k | 🟦 空闲 | 11-QUICKDELTA-ENHANCE |
| 7 | 15-DOCS | common | 40k | 🟦 空闲 | 12-TEST-*, 13-TEST-*, 14-TEST-* |
| 7 | 16-RELEASE | quick | 20k | 🟦 空闲 | 15-DOCS |

**总计**: 16 个任务, ~545k tokens, 8 个 Wave

---

## 任务详情

### Wave-0: 基础设施

#### Task: 01-PREP - 项目准备

**Profile**: quick
**预估上下文**: ~20k tokens
**状态**: 🟦 空闲
**依赖**: 无

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md`
- `pyproject.toml`

**核心代码入口**:
- `pyproject.toml` - 更新版本号
- `.git/` - 创建开发分支

**Checklist**:
- [ ] 创建 `feature/v1.3` 开发分支
- [ ] 更新 `pyproject.toml` 版本为 `1.3.0-dev`
- [ ] 确认 uv sync 成功
- [ ] 确认现有测试通过

---

### Wave-1: 配置扩展

#### Task: 02-CONFIG-SCORING - 打分配置数据结构

**Profile**: common
**预估上下文**: ~30k tokens
**状态**: 🟦 空闲
**依赖**: 01-PREP

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 2 节打分机制
- `src/cc_spec/core/config.py`

**核心代码入口**:
- `src/cc_spec/core/config.py`

**Checklist**:
- [ ] 创建 `Dimension` 枚举类
- [ ] 创建 `DimensionConfig` 数据类
- [ ] 创建 `ScoringConfig` 数据类
- [ ] 在 `Config` 中添加 `scoring` 字段
- [ ] 实现 `ScoringConfig.from_dict()` 方法
- [ ] 实现 `ScoringConfig.to_dict()` 方法
- [ ] 添加默认维度配置
- [ ] 更新 `load_config()` 支持 v1.3 格式

---

#### Task: 03-CONFIG-LOCK - 锁配置数据结构

**Profile**: common
**预估上下文**: ~25k tokens
**状态**: 🟦 空闲
**依赖**: 01-PREP

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 3 节 Git 锁
- `src/cc_spec/core/config.py`

**核心代码入口**:
- `src/cc_spec/core/config.py`

**Checklist**:
- [ ] 创建 `LockConfig` 数据类
- [ ] 在 `Config` 中添加 `lock` 字段
- [ ] 实现 `LockConfig.from_dict()` 方法
- [ ] 实现 `LockConfig.to_dict()` 方法
- [ ] 添加默认锁配置 (timeout=30, use_git_commit=false)

---

### Wave-2: 核心模块

#### Task: 04-SCORING-REFACTOR - 四维度打分重构

**Profile**: heavy
**预估上下文**: ~60k tokens
**状态**: 🟦 空闲
**依赖**: 02-CONFIG-SCORING

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 2 节完整内容
- `src/cc_spec/core/scoring.py` - 现有实现
- `src/cc_spec/core/config.py` - ScoringConfig

**核心代码入口**:
- `src/cc_spec/core/scoring.py`

**Checklist**:
- [ ] 创建 `DimensionScore` 数据类
- [ ] 创建 `TaskScore` 数据类
- [ ] 创建 `ChecklistResult` 数据类
- [ ] 实现 `_classify_item()` 关键词分类函数
- [ ] 实现 `calculate_task_score()` 单任务打分
- [ ] 实现 `calculate_checklist_result()` 整体打分
- [ ] 实现 `format_dimension_report()` 维度报告格式化
- [ ] 实现 `generate_failure_report_v13()` 增强的失败报告
- [ ] 保持向后兼容：旧版 `calculate_score()` 可用

---

#### Task: 05-LOCK-MODULE - 锁模块实现

**Profile**: common
**预估上下文**: ~50k tokens
**状态**: 🟦 空闲
**依赖**: 03-CONFIG-LOCK

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 3 节完整内容

**核心代码入口**:
- `src/cc_spec/core/lock.py` (新建)

**Checklist**:
- [ ] 创建 `src/cc_spec/core/lock.py` 文件
- [ ] 实现 `LockInfo` 数据类
- [ ] 实现 `LockInfo.is_expired()` 方法
- [ ] 实现 `LockInfo.to_json()` 方法
- [ ] 实现 `LockInfo.from_json()` 方法
- [ ] 实现 `LockManager` 类
- [ ] 实现 `LockManager.acquire()` 方法
- [ ] 实现 `LockManager.release()` 方法
- [ ] 实现 `LockManager.get_lock_info()` 方法
- [ ] 实现 `LockManager.cleanup_expired()` 方法
- [ ] 确保目录 `.cc-spec/locks/` 自动创建

---

#### Task: 06-STATE-ENHANCE - 状态增强

**Profile**: common
**预估上下文**: ~30k tokens
**状态**: 🟦 空闲
**依赖**: 01-PREP

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 4 节
- `src/cc_spec/core/state.py`

**核心代码入口**:
- `src/cc_spec/core/state.py`

**Checklist**:
- [ ] `TaskInfo` 添加 `agent_id` 字段
- [ ] `TaskInfo` 添加 `started_at` 字段
- [ ] `TaskInfo` 添加 `completed_at` 字段
- [ ] `TaskInfo` 添加 `error` 字段
- [ ] `TaskInfo` 添加 `retry_count` 字段
- [ ] 更新 `load_state()` 解析新字段
- [ ] 更新 `update_state()` 保存新字段
- [ ] 保持向后兼容：旧版 status.yaml 可读取

---

### Wave-3: 执行器集成

#### Task: 07-EXECUTOR-LOCK - 执行器锁集成

**Profile**: common
**预估上下文**: ~40k tokens
**状态**: 🟦 空闲
**依赖**: 05-LOCK-MODULE, 06-STATE-ENHANCE

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 3.3 节
- `src/cc_spec/subagent/executor.py`
- `src/cc_spec/core/lock.py`

**核心代码入口**:
- `src/cc_spec/subagent/executor.py`

**Checklist**:
- [ ] 导入 `LockManager`
- [ ] `SubAgentExecutor.__init__()` 初始化 `LockManager`
- [ ] 实现 `_execute_task_with_lock()` 方法
- [ ] 修改 `execute_wave()` 使用锁
- [ ] 启动时调用 `cleanup_expired()` 清理过期锁
- [ ] 锁获取失败时返回合适的错误信息

---

#### Task: 08-EXECUTOR-RESULT - 执行结果增强

**Profile**: quick
**预估上下文**: ~25k tokens
**状态**: 🟦 空闲
**依赖**: 06-STATE-ENHANCE

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 4 节
- `src/cc_spec/subagent/executor.py`

**核心代码入口**:
- `src/cc_spec/subagent/executor.py`

**Checklist**:
- [ ] `ExecutionResult` 添加 `agent_id` 字段
- [ ] `ExecutionResult` 添加 `wave` 字段
- [ ] `ExecutionResult` 添加 `retry_count` 字段
- [ ] 执行时生成唯一 `agent_id`
- [ ] 更新 `ResultCollector` 记录新字段

---

### Wave-4: 命令更新

#### Task: 09-CMD-CHECKLIST - checklist 命令更新

**Profile**: common
**预估上下文**: ~45k tokens
**状态**: 🟦 空闲
**依赖**: 04-SCORING-REFACTOR

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 2.4 节输出格式
- `src/cc_spec/commands/checklist.py`
- `src/cc_spec/core/scoring.py`

**核心代码入口**:
- `src/cc_spec/commands/checklist.py`

**Checklist**:
- [ ] 导入新的打分函数和数据类
- [ ] 修改打分逻辑使用 `calculate_checklist_result()`
- [ ] 显示四维度得分表格
- [ ] 显示未通过维度的详细信息
- [ ] 生成增强的失败报告 (checklist-result.md)
- [ ] 添加 `--dimension` 选项过滤显示
- [ ] 更新 `--help` 说明

---

#### Task: 10-CMD-APPLY - apply 命令更新

**Profile**: common
**预估上下文**: ~40k tokens
**状态**: 🟦 空闲
**依赖**: 07-EXECUTOR-LOCK

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 3.3 节
- `src/cc_spec/commands/apply.py`

**核心代码入口**:
- `src/cc_spec/commands/apply.py`

**Checklist**:
- [ ] 导入 `LockManager` 和 `LockConfig`
- [ ] 创建执行器时传入 `LockManager`
- [ ] 显示锁状态信息
- [ ] 添加 `--force-unlock` 选项
- [ ] 添加 `--skip-locked` 选项跳过被锁任务
- [ ] 更新任务状态时记录 `agent_id`
- [ ] 更新 `--help` 说明

---

### Wave-5: 增强功能

#### Task: 11-QUICKDELTA-ENHANCE - quick-delta 增强

**Profile**: common
**预估上下文**: ~35k tokens
**状态**: 🟦 空闲
**依赖**: 01-PREP

**必读文档**:
- `docs/plan/cc-spec-v1.3/03-设计方案.md` - 第 5 节
- `src/cc_spec/commands/quick_delta.py`

**核心代码入口**:
- `src/cc_spec/commands/quick_delta.py`

**Checklist**:
- [ ] 创建 `FileChange` 数据类
- [ ] 实现 `_parse_git_diff()` 函数
- [ ] 解析 `git diff --staged --name-status`
- [ ] 解析 `git diff HEAD~1 --name-status` (降级)
- [ ] 正确处理 A/M/D/R 状态码
- [ ] 处理 RENAMED 的新旧文件名
- [ ] 更新输出格式包含变更类型表格
- [ ] 添加变更统计 (ADDED/MODIFIED/REMOVED 计数)
- [ ] 添加行数统计 (+/-) 如果可用

---

### Wave-6: 测试补充

#### Task: 12-TEST-SCORING - 打分机制测试

**Profile**: quick
**预估上下文**: ~30k tokens
**状态**: 🟦 空闲
**依赖**: 09-CMD-CHECKLIST

**必读文档**:
- `src/cc_spec/core/scoring.py`
- `tests/test_scoring.py` (现有)

**核心代码入口**:
- `tests/test_scoring_v13.py` (新建)

**Checklist**:
- [ ] 测试 `_classify_item()` 关键词分类
- [ ] 测试 `calculate_task_score()` 单任务打分
- [ ] 测试 `calculate_checklist_result()` 整体打分
- [ ] 测试权重计算正确性
- [ ] 测试阈值判断逻辑
- [ ] 测试空 checklist 处理
- [ ] 测试向后兼容性

---

#### Task: 13-TEST-LOCK - 锁机制测试

**Profile**: quick
**预估上下文**: ~30k tokens
**状态**: 🟦 空闲
**依赖**: 10-CMD-APPLY

**必读文档**:
- `src/cc_spec/core/lock.py`

**核心代码入口**:
- `tests/test_lock.py` (新建)

**Checklist**:
- [ ] 测试 `LockInfo` 序列化/反序列化
- [ ] 测试 `LockInfo.is_expired()`
- [ ] 测试 `LockManager.acquire()` 成功获取
- [ ] 测试 `LockManager.acquire()` 锁被占用
- [ ] 测试 `LockManager.acquire()` 超时后获取
- [ ] 测试 `LockManager.release()`
- [ ] 测试 `LockManager.cleanup_expired()`
- [ ] 测试并发获取锁场景

---

#### Task: 14-TEST-QUICKDELTA - quick-delta 测试

**Profile**: quick
**预估上下文**: ~25k tokens
**状态**: 🟦 空闲
**依赖**: 11-QUICKDELTA-ENHANCE

**必读文档**:
- `src/cc_spec/commands/quick_delta.py`

**核心代码入口**:
- `tests/test_quick_delta_v13.py` (新建)

**Checklist**:
- [ ] 测试 `_parse_git_diff()` 解析 ADDED
- [ ] 测试 `_parse_git_diff()` 解析 MODIFIED
- [ ] 测试 `_parse_git_diff()` 解析 REMOVED
- [ ] 测试 `_parse_git_diff()` 解析 RENAMED
- [ ] 测试空 diff 处理
- [ ] 测试降级到 git log 场景

---

### Wave-7: 文档与发布

#### Task: 15-DOCS - 文档更新

**Profile**: common
**预估上下文**: ~40k tokens
**状态**: 🟦 空闲
**依赖**: 12-TEST-SCORING, 13-TEST-LOCK, 14-TEST-QUICKDELTA

**必读文档**:
- `README.md`
- `docs/` 目录

**核心代码入口**:
- `README.md`
- `docs/cc-spec/commands.md`
- `CHANGELOG.md`

**Checklist**:
- [ ] 更新 README.md 版本信息
- [ ] 添加 v1.3 新功能说明
- [ ] 更新命令参考文档
- [ ] 添加四维度打分说明
- [ ] 添加 Git 锁使用说明
- [ ] 添加 quick-delta 增强说明
- [ ] 更新 CHANGELOG.md

---

#### Task: 16-RELEASE - 发布准备

**Profile**: quick
**预估上下文**: ~20k tokens
**状态**: 🟦 空闲
**依赖**: 15-DOCS

**必读文档**:
- `pyproject.toml`
- `.github/workflows/` (如有)

**核心代码入口**:
- `pyproject.toml`

**Checklist**:
- [ ] 更新 `pyproject.toml` 版本为 `1.3.0`
- [ ] 运行完整测试套件
- [ ] 测试 GitHub 仓库安装
- [ ] 创建 Git tag `v1.3.0`
- [ ] 合并到 main 分支

---

## 统计信息

### Token 预估

| Wave | 任务数 | 总预估 | 并发节省 |
|------|--------|--------|----------|
| 0 | 1 | 20k | - |
| 1 | 2 | 55k | 27.5k |
| 2 | 3 | 140k | 46.7k |
| 3 | 2 | 65k | 32.5k |
| 4 | 2 | 85k | 42.5k |
| 5 | 1 | 35k | - |
| 6 | 3 | 85k | 28.3k |
| 7 | 2 | 60k | 30k |

**总计**: 545k tokens (串行), 约 337k tokens (并发优化后, 节省 ~38%)

### Profile 分布

| Profile | 任务数 | 说明 |
|---------|--------|------|
| quick | 6 | 简单任务，使用 haiku |
| common | 9 | 标准任务，使用 sonnet |
| heavy | 1 | 复杂任务，使用 opus |
