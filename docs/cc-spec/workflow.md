# 工作流详解

本文档详细介绍 cc-spec 的工作流程和最佳实践。

## 工作流概览

```
┌─────────┐    ┌─────────┐    ┌──────┐    ┌───────┐    ┌───────────┐    ┌─────────┐
│  init   │ -> │ specify │ -> │ plan │ -> │ apply │ -> │ checklist │ -> │ archive │
└─────────┘    └─────────┘    └──────┘    └───────┘    └───────────┘    └─────────┘
```

## 阶段详解

### 1. Init - 项目初始化

**目的**: 创建 cc-spec 项目结构

```bash
cc-spec init
```

**执行内容**:
1. 创建 `.cc-spec/` 目录
2. 下载/复制模板文件
3. 生成 `config.yaml`
4. 检测 AI 工具类型

**产出**:
```
.cc-spec/
├── config.yaml
├── templates/
│   ├── proposal.md
│   └── tasks.md
└── changes/
```

---

### 2. Specify - 需求规范

**目的**: 定义变更的需求和范围

```bash
cc-spec specify add-user-auth
```

**执行内容**:
1. 创建变更目录
2. 生成 `proposal.md` 模板
3. 初始化状态文件

**产出**:
```
.cc-spec/changes/add-user-auth/
├── proposal.md    # 需要填写需求
└── status.yaml    # 状态: SPECIFY
```

**下一步**: 编辑 `proposal.md`，填写需求详情

#### proposal.md 模板

```markdown
# 变更提案: add-user-auth

## 背景
[描述变更的背景和动机]

## 目标
[明确列出变更要达成的目标]

## 范围
[定义变更涉及的模块和文件]

## 技术方案
[描述实现方案的关键点]

## 验收标准
[定义如何验证变更完成]
```

---

### 3. Plan - 任务规划

**目的**: 将需求拆解为可执行的任务

```bash
cc-spec plan add-user-auth
```

**执行内容**:
1. 读取 `proposal.md`
2. 生成 `tasks.md` (Wave 分组)
3. 生成 `design.md` (技术决策)
4. 验证依赖关系

**产出**:
```
.cc-spec/changes/add-user-auth/
├── proposal.md
├── tasks.md      # 任务列表
├── design.md     # 技术决策
└── status.yaml   # 状态: PLAN
```

#### tasks.md 格式

```markdown
# Tasks - add-user-auth

## 概览

| Wave | Task-ID | 预估 | 状态 | 依赖 |
|------|---------|------|------|------|
| 0 | 01-SETUP | 30k | 🟦 空闲 | - |
| 1 | 02-MODEL | 50k | 🟦 空闲 | 01-SETUP |
| 1 | 03-API | 45k | 🟦 空闲 | 01-SETUP |
| 2 | 04-FE | 60k | 🟦 空闲 | 02-MODEL, 03-API |

## 任务详情

### 01-SETUP - Project Setup
**预估上下文**: ~30k tokens
**状态**: 🟦 空闲
**依赖**: 无

**Checklist**:
- [ ] 创建配置文件
- [ ] 添加环境变量
```

---

### 4. Apply - 任务执行

**目的**: 使用 SubAgent 并发执行任务

```bash
cc-spec apply add-user-auth --max-concurrent 3
```

**执行内容**:
1. 解析 `tasks.md`
2. 按 Wave 顺序执行
3. 同 Wave 内并发执行
4. 实时更新状态

**Wave 执行模式**:

```
Wave 0: [01-SETUP] ─────────────────────────>
                                              ↓
Wave 1: [02-MODEL] ─────>
        [03-API]   ─────> (并发执行)          ↓
                                              ↓
Wave 2: [04-FE]    ─────────────────────────>
```

**状态更新**:
- 🟦 空闲 → 🟨 进行中 → 🟩 完成 / 🟥 失败

---

### 5. Checklist - 验证完成

**目的**: 验证所有任务的 checklist 项

```bash
cc-spec checklist add-user-auth
```

**执行内容**:
1. 解析所有任务的 checklist
2. 计算完成得分
3. 生成验证报告

**通过条件**: 所有 checklist 项都已勾选 (或达到设定阈值)

---

### 6. Archive - 归档变更

**目的**: 归档已完成的变更

```bash
cc-spec archive add-user-auth
```

**执行内容**:
1. 检查 checklist 是否通过
2. 执行 Delta 合并 (如有)
3. 移动到 archive 目录

**产出**:
```
.cc-spec/changes/archive/2025-01-15-add-user-auth/
├── proposal.md
├── tasks.md
├── design.md
└── status.yaml   # 状态: ARCHIVE
```

---

## 快速修复工作流

对于小型修改，可以跳过完整流程：

```bash
cc-spec quick-delta "Fix typo in README"
```

这会:
1. 自动创建变更
2. 生成简化版 proposal
3. 直接归档

---

## 断点续传

如果执行过程中失败：

```bash
# 从失败处继续
cc-spec apply add-user-auth --resume
```

这会:
1. 检测已完成的 Wave
2. 从第一个未完成的 Wave 继续
3. 跳过已成功的任务

---

## 返工模式

如果需要重新执行某个任务：

```bash
# 标记任务需要返工
cc-spec clarify 02-MODEL

# 重新执行
cc-spec apply add-user-auth
```

---

## 状态转换图

```
SPECIFY ──> PLAN ──> APPLY ──> CHECKLIST ──> ARCHIVE
   ↑          ↑        │           │
   │          │        v           │
   │          └── CLARIFY <────────┘
   │                   │
   └───────────────────┘
```

- **SPECIFY**: 需求定义阶段
- **PLAN**: 任务规划阶段
- **APPLY**: 任务执行阶段
- **CHECKLIST**: 验证阶段
- **ARCHIVE**: 已归档
- **CLARIFY**: 返工模式 (可从任意阶段进入)

---

## 最佳实践

### 1. 需求规范

- 明确定义变更范围
- 列出验收标准
- 考虑边界情况

### 2. 任务拆分

- 单个任务 token 预估 < 100k
- 依赖关系清晰
- checklist 具体可验证

### 3. 并发执行

- 同 Wave 任务无相互依赖
- 合理设置 `--max-concurrent`
- 监控执行进度

### 4. 质量保障

- 所有 checklist 项都已完成
- 运行测试验证
- 代码 review

### 5. 归档管理

- 及时归档已完成变更
- 保持 changes 目录整洁
- 定期清理过期归档
